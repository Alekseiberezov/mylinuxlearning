nmap命令查看端口占用情况

使用yum -y install nmap来安装

![](./caozuo/install nmap.png)

![](./caozuo/nmap.png)

使用效果如图

netstat命令（sudo yum -y install net-tools）

语法：netstat -anp | grep 端口号

![](./caozuo/netstatgrep.png)

可以看出此处看的端口是https的端口